#!/usr/bin/with-contenv sh

if [ -z "$RCLONE_REMOTE_MOUNT" ]; then
  echo "Error: RCLONE_REMOTE_MOUNT environment variable was not passed to the container."
  exit 1
fi

if [ ! -f /rclone/.rclone.conf ]; then
  echo "Error: /rclone/.rclone.conf does not exist. You have to pass it from the host."
  exit 1
fi

mount_command="rclone mount --config=/rclone/.rclone.conf --allow-other --read-only --log-level INFO --log-file /rclone/rclone.log --rc $RCLONE_MOUNT_OPTIONS $RCLONE_REMOTE_MOUNT /rclone/remote"

echo "Executing mount rclone => $mount_command"
exec s6-setuidgid abc $mount_command
